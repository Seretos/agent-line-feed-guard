{
  "name": "agent-line-feed-guard",
  "version": "0.0.1",
  "description": "Repairs CRLF line endings in the files Claude Code parses, so a subagent, skill, or command never silently fails to load.",
  "author": {
    "name": "Arne von Appen"
  },
  "repository": "https://github.com/Seretos/agent-line-feed-guard",
  "skills": "./skills",
  "interface": {
    "displayName": "Agent Line Feed Guard",
    "shortDescription": "Repairs CRLF line endings in the files Claude Code parses, so a subagent, skill, or command never silently fails to load."
  }
}
